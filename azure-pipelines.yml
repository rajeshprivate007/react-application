trigger:
  - main   # or your branch name

pool:
  name: 'Default'   # My agent name

steps:
- checkout: self           # this fetches the code from your connected GitHub repo

- script: |
    echo "Running on $(Agent.Name)"
    ls -l
    pwd
  displayName: 'Verify agent and list files'
