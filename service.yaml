apiVersion: v1
kind: Service
metadata:
  name: react-app-service
spec:
  type: LoadBalancer  # Exposes the app publicly
  selector:
    app: react-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
